# Local development only. The Vite app calls this URL directly.
VITE_API_URL=http://localhost:8000

# Optional browser-error reporting. Never put private server-side credentials here.
VITE_SENTRY_DSN=
VITE_ENVIRONMENT=development
